LOCAL:
	Clonar datos desde github en mi pc -> git clone git@github.com:joserejon/drawercloud.git clonaciongit
	Conectarme al servidor -> ssh -i "claveAmazonEC2.pem" ubuntu@ec2-13-59-21-252.us-east-2.compute.amazonaws.com

SERVIDOR:
	sudo rm -R drawercloud_repository
	mkdir drawercloud_repository

LOCAL:
	Enviar código al servidor desde la carpeta clonada -> git push https://git-codecommit.us-east-2.amazonaws.com/v1/repos/drawercloud_repository

SERVIDOR:
	Clonar datos en el servidor -> git clone https://git-codecommit.us-east-2.amazonaws.com/v1/repos/drawercloud_repository
	Desactivar modo debug y cambiar a /var/www/static/ -> nano nano sitio_web/settings.py
	Copiar static -> sudo cp -r proyecto/static/ /opt/myenv/
	Ejecutar app -> gunicorn sitio_web.wsgi

